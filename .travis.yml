sudo: required

language: node_js

node_js:
- 12
- 14

cache:
  directories:
  - node_modules

before_install:
  - npm install -g codecov

script:
  - npm test -- --coverage
  - codecov

after_success:
  - bash <(curl -s https://codecov.io/bash)
