sudo: required

language: node_js

node_js:
- 8
- 10

cache:
  directories:
  - node_modules

before_install:
  - npm install -g codecov

script:
  - npm test -- --coverage
  - codecov

after_success:
  - bash <(curl -s https://codecov.io/bash)
